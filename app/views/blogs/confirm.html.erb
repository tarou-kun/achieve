<div class="container">
 <div class="col-md-6 col-md-offset-3 col-sm-10 col <-sm-offset-1></-sm-offset-1>">
  <h3>以下の内容で以下の内容で、送信する。</h3>
  <%= form_for(@blog) do |f| %>
  <!--'f.hidden_field'を使用して使用して、リクエストパラメータに値を値をできるようにしています。-->
  <%= f.hidden_field :title %>
  <%= f.hidden_field :content %>

  <!-- @blog.titleと@bllog.contentとすｓることで、表示票の値を画面に出力しています。-->
  <p>タイトル<%= @blog.title %></p>
  <p>本文<%= @blog.content %></p>
  <%= f.submit "登録する"%>
 <% end %>

 <%= form_for(@blog, url: new_blog_path, method: 'get') do |f| %>
  <%= f.hidden_field :title %>
  <%= f.hidden_field :content %>
  <%= f.submit "もどる", name: 'back' %>
 <% end %>
 </div>
</div>